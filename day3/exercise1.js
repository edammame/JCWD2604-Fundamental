//display the multiplication table of given integer
let number = 6;
for (let j = 1; j <= 10; j++) console.log(number, "x", j);

//check whether a string is a palindrome or not
let word = "madam";
let newWord = "";
for (let i = word.length - 1; i >= 0; i--) newWord += word[i];
console.log(word == newWord ? "palindrome" : "not palindrome");

//convert centimeter to kilometer
let cm = 500000;
console.log(cm / 100000, "km");

//format number as currency
let money = 1000000000;
if (true) console.log("Rp", money.toLocaleString("id-ID"));

//remmove the first occurence of a given "search string" from a string
 

//capitalize the first letter of each word in string

//reverse a string
let result = "hello";
let here = "";
for (let i = word.length; i > 0; i--) console.log(result);